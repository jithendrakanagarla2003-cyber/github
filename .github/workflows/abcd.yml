name : workflow810
on : workflow_dispatch

jobs :
  build-test :
    runs-on: ubuntu-24.04 
      - name: checkout
        uses: actions/checkout@v7

      - name: build
        run: npm install

      - name: test
        run: npm test
        
       - name: deploy
        run: echo "my code is deployed"
